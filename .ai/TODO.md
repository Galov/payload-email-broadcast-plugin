# TODO

## Текущ статус

Проектът вече има работещ plugin skeleton, админ структури, шаблони, тестово изпращане, реално изпращане в контролиран MVP вид, rich text съдържание, header/footer настройки и базова работа с групи.

Следващата голяма архитектурна задача е реалното broadcast изпращане да се премести върху Payload Jobs queue.

## Предстоящ ред на работа

1. Да се използва `.ai/IMPLEMENTATION_PLAN.md` като водещ ред за изпълнение.
2. Следващата фаза е Payload Jobs foundation за broadcast изпращане.
3. След всяка фаза да се пуснат описаните проверки.
4. След всяка фаза да се спре и да се даде отчет.

## Критични предпазни условия

- Да не се изпращат реални broadcast имейли без изрично потвърждение.
- Да не се прескачат фази.
- Да не се имплементират бъдещи фази предварително.
- Да не се пазят API keys в базата данни.
- Да не се hardcode-ват project-specific полета.
- Да не се правят commit, tag или push без изрична инструкция.
- Да не се променят лимити, batch size или send режими без изрично решение.

## Отворени задачи преди по-късните фази

- Да се имплементира Payload Jobs task `processEmailBroadcastBatch`.
- Да се промени real send endpoint-ът да queue-ва job, вместо да изпраща директно.
- Да се добави `queued` статус за кампании.
- Да се разширят `email-logs` статусите с `pending`, `sending` и `skipped`.
- Да се документира как jobs се стартират на Vercel и Hetzner.
- Да се проектира advanced recipient groups flow.
